name: 'job_market_analytics'
version: '1.0.0'
config-version: 2

profile: 'job_market_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  job_market_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table